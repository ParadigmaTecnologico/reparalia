import re
comunidad = "hola"
cadenaAnalizar = "hola fede"
#encontrado = re.search('(^|\s)'+comunidad+'($|\s)', re.escape(cadenaAnalizar), re.IGNORECASE)
encontrado = re.search('(^|\s)'+comunidad+'($|\s)', cadenaAnalizar, re.IGNORECASE)
if encontrado:
    print "encontrado"

